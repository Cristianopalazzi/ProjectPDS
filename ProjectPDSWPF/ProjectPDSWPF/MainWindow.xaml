<Window x:Class="ProjectPDSWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectPDSWPF"
        mc:Ignorable="d"
        Name="MyWindow"
        Title="PDSProject" Height="350" Width="Auto" SizeToContent="Width">
    <Grid>
        <TabControl Name="tabControl" Width="Auto" >
            <TabItem Header="File in ricezione" >
                <ListView Name="listReceivingFiles" ItemsSource="{Binding FilesToReceive}">

                    <!--<ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete" Click="Menu_delete_click"/>
                            <MenuItem Header="Modify" Click="Menu_modify_click"/>
                        </ContextMenu>
                    </ListView.ContextMenu>-->
                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </ListView.Resources>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="5">
                                            <Ellipse Height="54" Width="54">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding Image}"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <!--Serve per fare in modo che la colonna si adatti al contenuto-->
                                            <TextBlock Text="{Binding Name}" TextAlignment="Center" FontSize="16"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <!--Serve per fare in modo che la colonna si adatti al contenuto-->
                                        <TextBlock Text="{Binding Filename}" TextAlignment="Left" FontSize="16" Margin="5,0" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>


                            <GridViewColumn Width="240">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <ProgressBar Value="{Binding Value}"/>
                                            <TextBlock Text="{Binding Value, StringFormat={}{0:0}%}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Pic}" Width="32" Height="32"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        </GridView>
                    </ListView.View>



                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Name}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>



                </ListView>
            </TabItem>

            <TabItem Header="File in invio">
                <ListView Name="sendingFiles" ItemsSource="{Binding FilesToSend}">
                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </ListView.Resources>
                    <!--<ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete" Click="Menu_delete_click"/>
                            <MenuItem Header="Modify" Click="Menu_modify_click"/>
                        </ContextMenu>
                    </ListView.ContextMenu>-->

                    <ListView.View>
                        <GridView>
                            <GridViewColumn>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="5">
                                            <Ellipse Height="54" Width="54">
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding Immagine}"/>
                                                </Ellipse.Fill>
                                            </Ellipse>
                                            <TextBlock Text="{Binding Name}" TextAlignment="Center" FontSize="16"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <!--<GridViewColumn Header="File">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FileName}" TextAlignment="Left" FontSize="16"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>-->


                            <GridViewColumn Width="240">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <ProgressBar Value="{Binding Value}"/>
                                            <TextBlock Text="{Binding Value, StringFormat={}{0:0}%}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="140">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Cancel" Height="20" Width="100" Click="Cancel_Click">


                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Value}" Value="100">
                                                            <Setter Property="Visibility" Value="Hidden" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>



                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>


                            <GridViewColumn Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding Pic}" Width="32" Height="32"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                        </GridView>
                    </ListView.View>

                    <ListView.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock FontWeight="Bold" FontSize="14" Text="{Binding Name}"/>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </ListView.GroupStyle>



                </ListView>

            </TabItem>
            <TabItem Header="Contatti online" >
                <Grid>
                    <StackPanel>
                        <ListView x:Name="Neighbors" ItemsSource="{Binding NeighborsValues}" HorizontalAlignment="Stretch" VerticalAlignment="Top" >
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="5" HorizontalAlignment="Stretch" />
                                </ItemsPanelTemplate>

                            </ListView.ItemsPanel>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" VerticalAlignment="Stretch" Margin="10">
                                        <Ellipse Height="64" Width="64" >
                                            <Ellipse.Fill>
                                                <ImageBrush  ImageSource="{Binding NeighborImage}"  Stretch="Uniform" />
                                            </Ellipse.Fill>
                                        </Ellipse>
                                        <TextBlock Text="{Binding NeighborName}" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.ItemContainerStyle>
                                <Style  TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="BorderBrush" Value="Red" />
                                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                                </Style>
                            </ListView.ItemContainerStyle>
                        </ListView>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem Header="Impostazioni" Selector.Unselected="tabChanged">
                
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Row="0" Grid.Column="0" Content="AutoAccept" />
                        <Label Grid.Row="1" Grid.Column="0" Content="Default Directory" />
                        <Label Grid.Row="2" Grid.Column="0" Content="Default Directory Path" />
                        <CheckBox Grid.Row ="0" Grid.Column="2" Name="CheckAutoAccept" VerticalAlignment="Center"/>
                        <CheckBox Grid.Row ="1" Grid.Column="2" Name="CheckDefaultDir" VerticalAlignment="Center"/>
                        <Button Grid.Row="2" Grid.Column="1" Content="..." Margin="5" Click="openFolderBrowserDialog" />
                        <TextBox Grid.Row="2" Grid.Column="2" Name="defaultDirPath" IsReadOnly="true"/>
                    </Grid>
                   
              
            </TabItem>
        </TabControl>
    </Grid>
    <Window.Resources>

        <!--Serve per fare in modo che la colonna si adatti al contenuto-->
        <Style  TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="BorderBrush" Value="LightGray" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="FontSize" Value="16" />
        </Style>


    </Window.Resources>
</Window>